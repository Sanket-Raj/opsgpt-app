# 1. Install dependencies
pip install -r requirements.txt

# 2. Run local server with hot-reload
uvicorn app.main:app --reload

# 3. Test prediction endpoint
curl -X POST http://localhost:8000/predict -H "Content-Type: application/json" -d '{"text": "DevOps is"}'
